{
  "name": "My Codespace",

  // point to the Compose file inside .devcontainer
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",                // ← matches the new service above
  "workspaceFolder": "/workspace",

  "forwardPorts": [5432],

  "features": {
    "ghcr.io/devcontainers/features/docker-cli:1": {},
    "ghcr.io/devcontainers/features/postgres-cli:1": { "version": "16" }
  },

  "postCreateCommand": "pip install --upgrade pip psycopg2-binary"
}
